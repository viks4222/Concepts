
Standard Exception:
bad_typeid:
The bad_typeid exception is thrown by the typeid operator when the operand for typeid is a NULL pointer.
In other words, exceptions of class bad_typeid are thrown when typeid is called on a dereferenced null pointer.

bad_cast:
std::bad_cast is thrown when a dynamic_cast fails at runtime.


bad_alloc :
enough memory not available when memory allocated using new(runtime)

bad_typeid:
	derefrencing null pointer in tytpeid operator

